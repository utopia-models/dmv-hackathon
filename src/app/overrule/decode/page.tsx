import DecodeWizard from "@/components/overrule/DecodeWizard";

export const metadata = { title: "Decode your denial — Overrule" };

export default function DecodePage() {
  return <DecodeWizard />;
}
